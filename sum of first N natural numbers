#include <stdio.h>

int main() {
    // Write C code here
   int n, i , sum=0;
   printf("enter any number =");
   scanf("%d",&n);
   for(i=1;i<=n;i++)
   {
       sum = i+sum;
    
   }
  printf("sum of natural number: %d",sum);
    return 0;
}
