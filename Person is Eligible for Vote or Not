#include <stdio.h>

int main() {
    // Write C code here
   int age ;
   printf("enter the person Age :");
   scanf("%d",&age);
   if(age>=18)
   {
       printf("person is eligible");
   }
   else{
       printf("person is  not eligible");
   }
    return 0;
}
