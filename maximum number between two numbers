#include <stdio.h>

int main() {
    // Write C code here
    int a,b;
    printf("enter the number:");
    scanf("%d%d",&a,&b);
    if(a>b){
        printf("a is greater than b= %d" , a);
        
    }
    else{
         printf("a is not greater than b =%d ", b);
    }
    return 0;
}
